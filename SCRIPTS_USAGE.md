# 启动脚本使用说明

本项目提供了三个独立的启动和检测脚本，方便您单独管理前端和后端服务。

## 📁 脚本列表

| 脚本名称 | 功能描述 | 使用场景 |
|---------|---------|---------|
| `start-backend.sh` | 启动 KataGo 后端服务 | 单独启动后端，自动检测启动状态 |
| `start-frontend.sh` | 启动前端开发服务器 | 单独启动前端，会检查后端是否运行 |
| `check-backend.sh` | 检测后端服务状态 | 诊断后端问题，查看详细状态信息 |

---

## 🚀 使用方法

### 1. 启动后端服务

```bash
./start-backend.sh
```

**功能特性：**
- ✅ 自动检查必需文件（KataGo 可执行文件、模型、配置）
- ✅ 自动停止旧进程（如果端口被占用）
- ✅ 实时监控启动状态（最多等待 15 秒）
- ✅ 启动成功后显示服务信息和使用提示
- ✅ 启动失败时给出详细的错误信息和建议

**预期输出：**
```
🚀 启动 KataGo 后端服务...

✅ KataGo 可执行文件: server/bin/katago
✅ 模型文件: server/models/katago_model.bin.gz
✅ 配置文件: server/katago_config.cfg

📡 启动后端服务器 (端口 3001)...
⏳ 等待服务启动...
   [1/15] 等待服务响应...
   [2/15] KataGo 正在初始化...
   [3/15] KataGo 正在初始化...

✅ 后端服务启动成功！

================================================
  KataGo 后端服务已就绪
  后端地址: http://localhost:3001
  健康检查: http://localhost:3001/api/health
  进程 PID: 12345
================================================

💡 提示：
  - 使用 ./check-backend.sh 检查服务状态
  - 使用 pkill -f 'node.*katagoServer' 停止服务
  - 查看日志: tail -f server/logs/katago.log

按 Ctrl+C 停止服务
```

---

### 2. 启动前端服务

```bash
./start-frontend.sh
```

**功能特性：**
- ✅ 自动检查并安装依赖（如果 node_modules 不存在）
- ✅ 自动停止旧进程（如果端口被占用）
- ✅ 检查后端服务是否运行
- ✅ 如果后端未运行，会提示用户是否继续

**预期输出：**
```
🚀 启动前端应用...

✅ 依赖检查完成

🔍 检查后端服务...
✅ 后端服务已就绪

🎨 启动前端开发服务器...

================================================
  前端应用启动中...
  前端地址: http://localhost:5173
  后端地址: http://localhost:3001
================================================

按 Ctrl+C 停止服务
```

---

### 3. 检测后端状态

```bash
./check-backend.sh
```

**功能特性：**
- ✅ 检查 Node.js 和 KataGo 进程状态
- ✅ 检查端口 3001 是否被监听
- ✅ 检查健康检查接口响应
- ✅ 测试 AI 功能是否正常
- ✅ 检查必需文件是否存在
- ✅ 提供详细的诊断信息和修复建议

**预期输出：**
```
🔍 检查 KataGo 后端服务状态...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 进程检查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Node.js 后端进程正在运行
   PID: 12345, CPU: 0.5%, MEM: 1.2%

✅ KataGo 进程正在运行
   PID: 12346, CPU: 2.3%, MEM: 5.6%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔌 端口检查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 端口 3001 正在监听
   进程: node (PID: 12345)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏥 健康检查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 服务状态: ready
   消息: KataGo 已就绪

   完整响应:
   {"status":"ready","message":"KataGo 已就绪"}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 AI 功能测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
正在测试 AI 请求...
✅ AI 功能正常
   返回着法: D4

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 文件检查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ KataGo 可执行文件存在
   大小: 45M, 修改时间: Jan 3 10:00

✅ KataGo 模型文件存在
   大小: 234M, 修改时间: Jan 3 10:00

✅ KataGo 配置文件存在
   大小: 2.1K, 修改时间: Jan 3 10:00

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 状态总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Node.js 进程
✅ KataGo 进程
✅ 端口监听
✅ 健康检查
✅ 必需文件

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 后端服务运行正常！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔧 常见问题

### Q1: 后端启动超时怎么办？

**可能原因：**
1. KataGo 模型加载失败
2. 端口 3001 被占用
3. 配置文件错误

**解决方法：**
```bash
# 1. 运行检测脚本查看详细状态
./check-backend.sh

# 2. 停止所有相关进程
pkill -f "node.*katagoServer"
pkill -f "katago"

# 3. 重新启动
./start-backend.sh
```

---

### Q2: 如何查看后端日志？

```bash
# 查看实时日志
tail -f server/logs/katago.log

# 查看最近 50 行日志
tail -50 server/logs/katago.log
```

---

### Q3: 如何停止服务？

```bash
# 停止后端
pkill -f "node.*katagoServer"
pkill -f "katago"

# 停止前端
pkill -f "vite"

# 或者在运行脚本的终端按 Ctrl+C
```

---

### Q4: 端口被占用怎么办？

```bash
# 查看占用端口 3001 的进程
lsof -i :3001

# 停止占用进程
pkill -f "node.*katagoServer"

# 或者使用脚本（会自动处理）
./start-backend.sh
```

---

## 🎯 推荐工作流

### 开发调试流程

1. **启动后端**
   ```bash
   ./start-backend.sh
   ```

2. **检查后端状态**（可选）
   ```bash
   ./check-backend.sh
   ```

3. **启动前端**
   ```bash
   ./start-frontend.sh
   ```

4. **访问应用**
   - 前端：http://localhost:5173
   - 后端：http://localhost:3001

---

### 问题排查流程

1. **运行检测脚本**
   ```bash
   ./check-backend.sh
   ```

2. **根据检测结果采取行动**
   - 如果进程未运行 → 启动服务
   - 如果端口未监听 → 检查日志
   - 如果健康检查失败 → 重启服务

3. **查看详细日志**
   ```bash
   tail -f server/logs/katago.log
   ```

---

## 📝 注意事项

1. **首次运行前**，请确保已安装所有依赖：
   ```bash
   npm install
   cd server && npm install
   ```

2. **KataGo 模型文件**较大（约 234MB），首次下载可能需要一些时间

3. **后端启动**需要 2-5 秒加载模型，请耐心等待

4. **前端热重载**功能已启用，修改代码后会自动刷新

5. 如果遇到问题，请先运行 `./check-backend.sh` 进行诊断

---

## 🆘 获取帮助

如果以上方法都无法解决问题，请：

1. 运行 `./check-backend.sh` 并保存输出
2. 查看日志文件 `server/logs/katago.log`
3. 提供详细的错误信息以便排查

---

**祝您使用愉快！** 🎉
